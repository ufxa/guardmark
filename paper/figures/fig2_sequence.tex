% Figure 2 — GuardMark Protocol: Sequence Diagram
% Participants: Owner, GuardMark Engine, Protected Model, Verifier
% Color code: blue solid = watermark injection; green dashed = fingerprint embed;
%             orange dot-dash = ownership verification; red dotted = attack

\begin{tikzpicture}[
  font=\scriptsize,
  >=Stealth, thick,
  % lifeline styles
  lline/.style={draw=black!40, thin, dashed},
  % message styles
  wm/.style={blue!70!black, solid, ->},
  fp/.style={green!60!black, dashed, ->},
  ov/.style={orange!80!black, dash dot, ->},
  att/.style={red!70!black, dotted, ->},
  % box styles
  proc/.style={draw=black!70, fill=white, rounded corners=2pt,
               text width=2.6cm, align=center, font=\scriptsize},
  phase/.style={draw=black!30, fill=gray!8, dashed,
                inner xsep=4pt, inner ysep=2pt},
  head/.style={draw=black!80, fill=#1!20, rounded corners=3pt,
               minimum width=2.6cm, minimum height=0.55cm, align=center}
]

% ── X positions of lifelines ─────────────────────────────────────────────
\def\xOwner{0}
\def\xEngine{3.5}
\def\xModel{7.0}
\def\xVerify{10.5}

% ── Participant headers ──────────────────────────────────────────────────
\node[head=blue]   at (\xOwner,  0) {\textbf{Model Owner}};
\node[head=cyan]   at (\xEngine, 0) {\textbf{GuardMark Engine}};
\node[head=green]  at (\xModel,  0) {\textbf{Protected Model $\mathcal{M}^*$}};
\node[head=orange] at (\xVerify, 0) {\textbf{Verifier / Court}};

% ── Lifelines ────────────────────────────────────────────────────────────
\draw[lline] (\xOwner,  -0.35) -- (\xOwner,  -16.0);
\draw[lline] (\xEngine, -0.35) -- (\xEngine, -16.0);
\draw[lline] (\xModel,  -0.35) -- (\xModel,  -16.0);
\draw[lline] (\xVerify, -0.35) -- (\xVerify, -16.0);

% ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
% PHASE 1 — Watermark Injection
% ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
\node[phase, fit={(-0.6,-0.7) (11.0,-6.2)}, label=above left:{\textbf{Phase 1: Watermark Injection}}]
  {};

\draw[wm] (\xOwner,-1.2) -- node[above, font=\scriptsize]{$\mathcal{K}$, $\delta$, $\gamma$} (\xEngine,-1.2);
\node[proc] at (\xEngine,-1.8) {Key schedule:\\$\mathcal{G}_t \leftarrow h(\mathcal{K} \| t)$};

\draw[wm] (\xEngine,-2.5) -- node[above, font=\scriptsize]{$\Delta\text{-logit bias}$} (\xModel,-2.5);
\node[proc] at (\xModel,-3.1) {$\ell_t[w] \mathrel{+}= \delta$\\$\forall w \in \mathcal{G}_t$};

\draw[fp] (\xOwner,-3.9) -- node[above, font=\scriptsize]{$\mathcal{F} = \{(p_i, r_i)\}_{i=1}^{N}$} (\xEngine,-3.9);
\node[proc] at (\xEngine,-4.5) {Embed fingerprint\\trigger--response pairs};

\draw[fp] (\xEngine,-5.2) -- node[above, font=\scriptsize]{LoRA adapter $\Delta W_\mathcal{F}$} (\xModel,-5.2);

\node[draw=blue!50, fill=blue!5, rounded corners, font=\scriptsize, text width=2.4cm, align=center]
  at (\xModel,-5.8) {Protected Model\\$\mathcal{M}^*$ ready};

% ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
% PHASE 2 — Attack Simulation (Adversary box)
% ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
\node[phase, draw=red!40, fill=red!3,
  fit={(-0.6,-6.8) (11.0,-9.5)},
  label=above left:{\textbf{Phase 2: Adversarial Attack} (optional)}] {};

\draw[att] (\xModel,-7.3) -- node[above, font=\scriptsize]{re-fine-tune / prune / quantize} (\xOwner,-7.3);
\node[proc, draw=red!50, fill=red!5] at (\xEngine,-8.1) {Suspect model\\$\hat{\mathcal{M}}$ generated};
\draw[att, <->] (\xEngine-0.2,-8.1) -- (\xModel-0.2,-8.8);
\node[font=\scriptsize, text=red!70] at (5.2,-9.0) {$\hat{\mathcal{M}} \approx \mathcal{M}^*$};

% ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
% PHASE 3 — Ownership Verification
% ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
\node[phase, draw=orange!40, fill=orange!3,
  fit={(-0.6,-10.0) (11.0,-15.5)},
  label=above left:{\textbf{Phase 3: Ownership Verification}}] {};

\draw[ov] (\xOwner,-10.5) -- node[above, font=\scriptsize]{Claim $+$ $\mathcal{K}$} (\xVerify,-10.5);
\draw[ov] (\xVerify,-11.2) -- node[above, font=\scriptsize]{Query $\hat{\mathcal{M}}$} (\xEngine,-11.2);

\node[proc] at (\xEngine,-11.9) {Statistical test\\$z$-score $> z_\alpha$?};
\draw[ov] (\xEngine,-12.5) -- node[above, font=\scriptsize]{WIA score} (\xVerify,-12.5);

\draw[ov] (\xVerify,-13.2) -- node[above, font=\scriptsize]{Fingerprint probes $\mathcal{F}$} (\xEngine,-13.2);
\node[proc] at (\xEngine,-13.8) {Cosine sim\\$\geq \tau$?};
\draw[ov] (\xEngine,-14.4) -- node[above, font=\scriptsize]{FVA score} (\xVerify,-14.4);

\node[proc, draw=orange!60, fill=orange!10] at (\xVerify,-15.1)
  {$\text{WRI} = \alpha p_d + \beta(1-p_f) + \gamma R$\\Claim verdict};

% ── Legend ───────────────────────────────────────────────────────────────
\begin{scope}[xshift=0cm, yshift=-16.5cm]
  \draw[wm]  (0,0) -- (0.7,0) node[right]{Statistical watermark (WIA)};
  \draw[fp]  (4.0,0) -- (4.7,0) node[right]{Behavioral fingerprint (FVA)};
  \draw[ov]  (8.5,0) -- (9.2,0) node[right]{Ownership verification (OCA)};
  \draw[att] (0,-0.5) -- (0.7,-0.5) node[right]{Adversarial attack};
\end{scope}

\end{tikzpicture}
